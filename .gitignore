# Local config (contains sysop password hash / fido credentials) — see VirtBBS.DAT.example
/VirtBBS.DAT

# Local SQLite database (runtime data)
/VirtBBS.db

# Build artifacts
/virtbbs
/releases/
/updates/
/update/

# Go
*.test
*.out

# Android / Gradle
android/**/build/
android/**/.gradle/
android/**/.kotlin/
android/**/local.properties

# Runtime data (local DB, TLS keys, SQLite WAL sidecars)
/data/
/logs/*.log
/logs/*.DAT
/files/
/fido/

# Runtime-generated BinkP stats bulletins (refreshed at midnight)
display/BINKPDAY.ANS
display/BINKPALL.ANS

# Local FidoNet operational files (not part of source; repo root only)
/ROUTES.BBS
/NODELIST.*
/NODEDIFF.*

# OS / editor
.DS_Store
*.swp
